{
  "name": "rotating-discord-widget-independent-slots",
  "version": "1.0.0",
  "private": true,
  "description": "Discord widget with 4 slots, each holding its own independent list of name+description+image entries that rotate on its own schedule every 6 hours.",
  "main": "widget.js",
  "scripts": {
    "start": "node widget.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5"
  }
}
